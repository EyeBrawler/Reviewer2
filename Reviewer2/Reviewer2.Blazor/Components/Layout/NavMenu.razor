@implements IDisposable

@inject NavigationManager NavigationManager

<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid align-middle align-items-center">
        <img src="favicon.png" class="img-fluid w-25" alt="...">
        <a class="navbar-brand" href="">Reviewer2</a>
    </div>
</div>

<input type="checkbox" title="Navigation menu" class="navbar-toggler" />

<div class="nav-scrollable" onclick="document.querySelector('.navbar-toggler').click()">
    <nav class="nav flex-column h-100">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <div class = "align-middle">
                    <i class="bi bi-house-door"></i>
                    Home
                </div>
            </NavLink>
        </div>

        <AuthorizeView>
            <Authorized>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="Account/Manage">
                        <div class = "align-middle">
                            <i class="bi bi-person-circle"></i>
                            @context.User.Identity?.Name
                        </div>
                    </NavLink>
                </div>
                
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="conference/register">
                        <div class="align-middle">
                            <i class="bi bi-calendar-check"></i>
                            Conference Registration
                        </div>
                    </NavLink>
                </div>
                
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="submit">
                        <div class="align-middle">
                            <i class="bi bi-upload"></i>
                            Submit Paper
                        </div>
                    </NavLink>
                </div>
                
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="submissions">
                        <div class="align-middle">
                            <i class="bi bi-newspaper"></i>
                            Submissions
                        </div>
                    </NavLink>
                </div>
                
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="review-dashboard">
                        <div class="align-middle">
                            <i class="bi bi-clipboard-check"></i>
                            Paper Review
                        </div>
                    </NavLink>
                </div>
                
                <AuthorizeView Roles="Admin,PaperChair">
                    <Authorized Context="authContext">
                        <div class="nav-item px-3">
                            <NavLink class="nav-link" href="assignments">
                                <div class="align-middle">
                                    <i class="bi bi-people-fill"></i>
                                    Review Assignments
                                </div>
                            </NavLink>
                        </div>
                    </Authorized>
                </AuthorizeView>
                
                <div class="nav-item px-3">
                    <form action="Account/Logout" method="post">
                        <AntiforgeryToken />
                        <input type="hidden" name="ReturnUrl" value="" />
                        <button type="submit" class="nav-link">
                            <div class="align-middle">
                                <i class="bi bi-box-arrow-right"></i>
                                Logout
                            </div>
                            
                        </button>
                    </form>
                </div>
            </Authorized>
            <NotAuthorized>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="Account/Register">
                        <span class="bi bi-person-nav-menu" aria-hidden="true"></span> Register
                    </NavLink>
                </div>
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="Account/Login">
                        <span class="bi bi-person-badge-nav-menu" aria-hidden="true"></span> Login
                    </NavLink>
                </div>
            </NotAuthorized>
        </AuthorizeView>
        <div class="mt-auto">
            <AuthorizeView Roles="Admin, ConferenceChair">
                <div class="nav-item px-3">
                    <NavLink class="nav-link" href="admin">
                        <div class="align-middle">
                            <i class="bi bi-shield-lock"></i>
                            Admin Panel
                        </div>
                    </NavLink>
                </div>
            </AuthorizeView>
        </div>
    </nav>
</div>

@code {
    private string? currentUrl;

    protected override void OnInitialized()
    {
        currentUrl = NavigationManager.ToBaseRelativePath(NavigationManager.Uri);
        NavigationManager.LocationChanged += OnLocationChanged;
    }

    private void OnLocationChanged(object? sender, LocationChangedEventArgs e)
    {
        currentUrl = NavigationManager.ToBaseRelativePath(e.Location);
        StateHasChanged();
    }

    public void Dispose()
    {
        NavigationManager.LocationChanged -= OnLocationChanged;
    }
}

